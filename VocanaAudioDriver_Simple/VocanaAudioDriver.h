// VocanaAudioDriver.h
// DriverKit audio driver header (not currently used - HAL plugin approach preferred)
//
// This would be used for a DriverKit dext approach, but we're using HAL plugin instead.
// DriverKit requires special entitlements and dext signing which is more complex.